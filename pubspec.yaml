name: reactive_forms_generator

workspace:
  - packages/generator_tests
  - packages/reactive_forms_annotations
  - packages/reactive_forms_generator
  - packages/reactive_forms_generator/example

environment:
  sdk: ">=3.6.0 <4.0.0"

dependency_overrides:
  test_api: 0.7.4

dev_dependencies:
  build_runner: ^2.6.0
  melos: ^7.0.0-dev.7
